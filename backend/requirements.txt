# ── Core API ──────────────────────────────────────────────
# Phase 16 pin review: prefer == for direct app deps; >= floors for SDKs
# that publish frequent security patches. Re-run `pip-audit` before GA.
fastapi==0.135.0
uvicorn[standard]==0.32.1
python-multipart>=0.0.31
starlette>=1.1.0
python-dotenv==1.0.1
pydantic-settings>=2.0.0,<3

# ── Database ──────────────────────────────────────────────
sqlmodel>=0.0.21,<0.1
psycopg2-binary>=2.9.9,<3
alembic>=1.13.0,<2

# ── Auth & Security ───────────────────────────────────────
python3-saml>=1.16.0,<2
lxml>=5.0.0,<6
python-jose[cryptography]>=3.4.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
cryptography>=48.0.1,<49
pyotp>=2.9.0,<3

# ── AI / LLM ─────────────────────────────────────────────
openai>=1.0.0
# Optional: pip install anthropic  (Anthropic is also callable via httpx)

# ── HTTP & WebSocket ─────────────────────────────────────
httpx==0.27.0
aiohttp>=3.9.0
websockets>=12.0

# ── Cloud & Tool Connectors ───────────────────────────────
boto3>=1.34.0
kubernetes>=29.0.0
jira>=3.8.0
PyGithub>=2.1.0
pagerduty>=3.0.0

# ── Task Queue ───────────────────────────────────────────
celery[redis]>=5.4.0
redis>=5.0.0

# ── Observability & Rate Limiting ────────────────────────
prometheus-client>=0.20.0
slowapi==0.1.9
sentry-sdk==2.3.1

# ── Scheduling ───────────────────────────────────────────
apscheduler==3.10.4

# ── Utilities ────────────────────────────────────────────
pyyaml>=6.0.1
markdown>=3.5.0
openpyxl>=3.1.0
reportlab>=4.0.0,<5

# ── Testing ──────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-mock>=3.12.0
respx>=0.21.0
pip-audit>=2.6.0
