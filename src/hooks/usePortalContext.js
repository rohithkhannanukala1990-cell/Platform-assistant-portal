/**
 * @deprecated Import from `../contexts/PortalContext` instead.
 * Re-exports global portal workspace + environment helpers.
 */
export { usePortalContext, PortalProvider } from '../contexts/PortalContext.jsx'
