[pytest]
markers =
    smoke: enterprise beta smoke checks (health, login, llm, github)
    prod_e2e: production-like agent E2E + HITL (Phase P4)
    p5: competitor gap closures (scorecards, postmortems, catalog actions)
